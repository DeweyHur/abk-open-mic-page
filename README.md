# ABK Open Mic Page

Interactive GitHub Pages dashboard for the upcoming ABK open mic setlist.

## Local Preview

```powershell
python -m http.server 8000
```

Open `http://localhost:8000/`.

## Deploy

Push this repository to GitHub and enable GitHub Pages for the branch that contains `index.html`.
